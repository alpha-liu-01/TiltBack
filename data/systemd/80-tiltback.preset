# pmOS / systemd 99-default.preset is "disable *". Without this, the
# post-install enable is undone by postmarketos-base-systemd's preset trigger.
enable tiltback-greeter.service
enable tiltback-greeter.path
